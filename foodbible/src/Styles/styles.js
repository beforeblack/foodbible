import { StyleSheet, Platform } from 'react-native';

export default StyleSheet.create({
  container: {
    height: 40,
    width: '80%',
    borderRadius: 4,
    borderWidth: 4,
    borderColor: '#d6d7da',
    justifyContent: 'center',
  },
  containerText: {
    fontSize: 20,
    textAlign: 'center'
  }
});
